[package]
name = "jems-node"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features=["derive"] }
anyhow = "1.0"
tokio = { version = "1", features=["macros", "rt-multi-thread"] }
jems-core = { path = "../jems-core" }
jems-rpc = { path = "../jems-rpc" }
toml = "0.8"
