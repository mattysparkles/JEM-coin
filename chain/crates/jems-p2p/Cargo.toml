[package]
name = "jems-p2p"
version = "0.1.0"
edition = "2021"

[dependencies]
libp2p = { version = "0.53", features = ["gossipsub", "mdns", "tcp", "noise", "yamux", "tokio", "macros"] }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
async-trait = "0.1"
