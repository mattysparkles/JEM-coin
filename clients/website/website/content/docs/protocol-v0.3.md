# JEMs Protocol v0.3

For up-to-date parameters and details, see the main docs copy in `/docs/protocol-v0.3`.

This bundled copy ensures the whitepaper and docs continue to render even if external docs aren’t available at runtime.

